> [!IMPORTANT]
> **历史快照**：本文记录 2026-08-26/27 当时的审计或实施状态，不再作为当前产品说明。2026-08-28 后的信息架构、AI 默认权限、PWA 使用方式等已经继续调整；当前行为以 `main` 代码、根目录 `README.md` 与 `docs/*.md` 为准。

# LifeOS 产品体验整改实施答卷

> 实施日期：2026-08-26；可靠性续修：2026-08-27
>
> 依据：`PRODUCT_UX_AUDIT_2026-08-26.md`
>
> 原则：本地优先、数据兼容、诚实状态、一个主动作、主流程优先

## 结果概览

本轮将产品主线收敛为 **收集 → 安排 → 专注 → 沉淀 → 回顾**，优先完成审计中的 P0 信任问题，并把 P1 的核心接力关系落到共享查询、真实动作和统一数据关联上。隐藏功能没有删除数据或破坏旧链接；旧计费项目、文档实验编辑器等仍可通过既有深链读取，但不会继续进入默认产品入口。

## P0：可靠、诚实、可恢复

- **笔记编辑器契约**：所有空白笔记现在都会生成含空段落的合法 Lexical 状态；首页快速收集、普通新建、版本恢复与外部内容写入共用同一规范化边界。旧 `root.children: []` 数据会自动修复，避免打开笔记时触发 `setEditorState` 崩溃，同时保留已有富文本与纯文本索引。
- **收藏恢复**：收藏持久化升级到 v4；旧数组、对象、双重 JSON、无效日期、缺失字段和大库逐条修复，不因一条坏数据清空整个库。新增空库、旧数据、1,000 条、归档、回收站和恢复测试；错误边界可导出不含内容与密钥的诊断 JSON。
- **真实空数据**：Today 的 `0/0` 显示 `— / 暂无计划`；项目 SPI、按时率、趋势和健康度在没有基线或样本时显示“数据不足”；Portfolio 空项目不再判红。
- **动作兑现**：晚间“移到明天 / 一周后 / 放弃并归档”真实修改不同状态；Rollover 的“保留在积压”和“放弃”不再同义；周回顾会创建并打开真实笔记。
- **页面壳层**：Availability 只保留全局页头；Focus、绘图、表单编辑/填写/回复使用独立全屏壳层，避免双页头与 `h-screen` 嵌套。
- **功能分层**：默认入口隐藏发票、计费、报表、Quest、XP、闪卡、表格、演示、绘图和表单；旧数据与深链继续兼容。
- **本地优先文案**：账户改为“个人资料（仅存本机）”，移除恢复邮箱；统一说明“核心数据留在本机，可选 AI/天气能力仅在主动启用时联网”；API key 文案与设备密钥实现一致。

## P1：主工作流闭环

- **断链动作修复**：首页“开始专注”会携带当前任务，显式停止后不会自动重开计时；Today 的事件/时段会在日程中定位、编辑或按所选小时新建；项目卡“添加下一步”会打开真实任务创建并安全预填项目。
- **导出与编辑动作**：三种笔记布局的 PDF 菜单均接入真实导出服务并处理重复点击与错误；实验文档工具栏的文字颜色、高亮和导出由空点击改为可访问的受控菜单。
- **收件箱**：逐条选择项目、日期和优先级；接受为待办、稍后提醒、归档、转笔记、含 URL 时转收藏；支持 `J/K`、`1–3`、`Enter`、`S`、`N`、`A`；清零态可回今天或继续收集。
- **Today Query**：首页、Today 和晨间规划共用同一查询：未完成的逾期、今日到期、进行中和等待复核任务；排除归档，并为日期边界补单元测试。
- **项目中心**：项目页先显示项目目标、下一步、真实进度和创建入口，分析只在有数据时出现；时间记录选择器使用核心项目 ID，旧计费项目仅作为已归档历史名称保留。
- **专注会话**：Focus 是界面、番茄是时长策略、时间记录是唯一活动会话。开始/暂停/继续/停止共享一条记录，默认非计费；Focus 退出处理活动计时，并可“完成任务并结束专注”。
- **回顾**：默认显示最近 7 天成果与开放循环；相邻自动保存噪声会合并；任务和笔记使用精确深链；周回顾沉淀为真实笔记。
- **收藏到行动/知识**：收藏页新增“添加链接”主动作；每条收藏的触控可达“更多操作”可创建任务或保存为笔记。
- **笔记承接**：已有笔记时首次进入自动打开最近编辑项；真实空库提供“新建空白笔记 / 从模板创建”；模板使用本地显示名称；单篇右键菜单支持归档与取消归档。

## 信息架构与界面收敛

- 桌面侧栏固定今天、收件箱、工作、知识、回顾，AI 放在主线之后，高级能力进入“更多功能”。
- 移动底栏改为“今天 / 收件箱 / 任务 / 笔记 / 更多”；更多面板限制可视高度并可滚动到底。
- 首页补齐“沉淀”阶段；页面名统一为首页、今天、项目、收藏、文档、回顾。
- 任务页默认只显示收件箱与任务；搜索、看板/列表和一个“新建任务”为常显控制，其余进入“更多”。
- Availability 设置持久化并校验开始时间早于结束时间，动作准确命名为“复制空闲时段”。
- 404 在没有应用内历史时直接返回首页，不再尝试无效的 `navigate(-1)`。

## 2026-08-27 最终补齐

- **密钥可靠性**：设备管理的 API key 只接受带认证的 WebCrypto 密文；旧版、无版本或损坏数据会安全清理并要求重新配置，不再把随机可解码字节误认成有效密钥。
- **隐藏能力不再回流**：功能分层同时约束侧栏、命令面板、组件管理器、布局恢复和默认组件；绘图、表单、Quest、闪卡等封存能力不会从第二入口重新出现，旧数据与兼容直链仍保留。
- **精力闭环**：最近记录可查看、编辑和删除；推荐任务可直接安排到今天或一周后；“倦怠警报”改为非医疗化的“连续低精力提醒”。
- **诚实周回顾**：无任务、习惯、日程或时间证据时显示“数据不足”，不再生成虚假积极结论；行动项可直接创建下周任务。
- **真实日回顾**：开放循环可决定明天、以后或放弃归档；学习、精力和下一步会沉淀到当日日记，并生成真实后续任务。
- **项目组合出口**：卡片、列表和时间线都可精确打开核心项目；空组合可直接进入项目创建。
- **自动化闭环**：增加安全模板、编辑、复制、无写入试运行、精确任务链接和失败重试；规则具备活跃、归档、回收站、恢复和永久删除状态，归档/删除规则不会执行。
- **内容生命周期**：单篇与批量笔记删除进入回收站，恢复和永久删除分离；删除文件夹只把笔记移到上级/根目录。活跃任务的“删除”改为可恢复归档，永久删除只在归档视图出现。文档同样具备归档、回收站、恢复和永久删除，并提供触控可达菜单。
- **设置收敛**：14 个一级分类收敛为 8 个稳定分类，旧查询参数自动迁移；增加设置搜索、影响范围和即时本机保存说明；全量备份与模块导入导出不再重复显示同一导入组件。
- **作品集与响应式动作**：Portfolio 支持精确项目深链；自动化和文档的关键动作不再只依赖悬停或右键。
- **浏览器验收清单**：17 个 `skip/fixme` 已清零，旧自动化测试夹具已迁移到当前 `trigger/conditions/actions` 契约；静态清单显示关键覆盖就绪。

### 可见体验升级

- 首页首屏从产品介绍改为当前行动驾驶舱：直接显示当前焦点、今日/待分拣/逾期真实计数，以及一个统一快速收集框。
- 五阶段工作循环固定为一行，不再在桌面端把“回顾”挤到第二行；今日空状态收紧为一个明确的“安排今天”动作。
- 使用高对比焦点区、轻量上下文卡和更紧凑的今日区建立视觉层级，减少同色大卡与无意义留白。
- 桌面侧栏由混合 Emoji 改为统一 Lucide 图标，强化当前项状态并统一展开/设置/主题等控制。
- AI 指挥中心降入「更多功能」，主导航只保留日常行动与知识闭环；快捷键和 `/ai` 深链继续可用。

## 设计系统

- 将遗留的 `primary-light/dark/cyan`、`bg-bg-*`、`text-text-secondary/tertiary`、未注册 surface hover/base/tertiary 等类机械映射到现有语义 token。
- 新增 `npm run lint:design-tokens`，并接入 `scripts/ci.sh`，阻止旧命名回流。
- 修复旧计时组件中粘连的 Tailwind 类名（如 `rounded-buttonhover:*`），避免看似存在但实际不生效的样式。

## 2026-08-27 交接补完（对“未完成”提交的收尾）

接手时基线：`b7bcf7a 未完成`，全部清单项已落地但验证发现 1 个失败测试与若干退回问题；本轮全部修复并通过全量验证。

- **修复失败测试**：`automationEngine.test.ts` 的“store action 抛错返回 false”用例仍在 mock `moveTask`，而实现已改为 `updateTask`（防递归触发）；mock 与断言同步更新。
- **真实计数**：首页“今日 N 项”改为全量计数（此前按截断后的 6 条显示，超过 6 条永远显示“今日 6 项”）。
- **共享 Today Query 收敛**：MyDayWidget 改为使用 `getTodayTasks`（补充逾期与归档排除）；智能排程的逾期比较从 UTC 改为本地日期键（修复 UTC+8 等时区跨日错位）。
- **专注/计时共享会话加固**：主计时 UI 默认 `billable:false`；`startTimer` 不再静默丢弃进行中的会话（先持久化再开新会话）；`stopTimer` 只清除自己被停止的会话，避免竞态清掉新会话；Focus 大屏计时在共享条目暂停时冻结显示。
- **自动化错误边界**：自动化引擎“异常返回 false”路径补测试。
- **统一归档/回收站补齐**：绘图与表单新增回收站（软删除/恢复/永久删除分离，表单回复保留到永久删除），删除确认文案按生命周期措辞；习惯“删除”改为可恢复归档，永久删除只在已归档列表；项目卡片归档增加确认；计时项目管理器改为在统一核心项目模型中新建/归档，旧计费项目仅保留历史删除。
- **隐藏能力防回流**：生产力评分组件（`productivitykarma`）映射到隐藏的 gamification 功能；widget 存储迁移 v10→v11 会剥离旧布局中的该组件；相关注释修正，避免再出现“可手动添加”的误导。
- **空状态诚实化**：个人分析的“完成率”在 0 样本时显示 `— / 暂无任务样本`；状态分布图在无样本时显示明确空态提示。
- **触控/键盘可达**：全站 65 处仅悬停可见的动作列统一增加 `group-focus-within`（键盘）与 `pointer-coarse`（触屏）揭示；文档文件夹增加常显“⋯”菜单；侧栏首页设置按钮改为 Lucide 图标并支持聚焦揭示。
- **命名与面包屑**：甘特图面包屑识别 `timeline` 标签；`/create` 面包屑与页头统一为“文档”；侧栏补齐自动化和项目组合的 Lucide 图标。
- **其他可靠性**：RolloverModal 空任务时改为渲染后通知父组件（不再在渲染期调用 setState）；链接库 rehydrate 改用 zustand `merge` 归一化（不再原地改写）；回收站 30 天清理承诺真正执行；面包屑/注释失实信息修正；云盘备份引导明确“上传由云盘客户端完成，LifeOS 不联网上传”。
- **新增回归测试**：widget 隐藏组件迁移、绘图/表单回收站生命周期、计时会话不被静默覆盖、特征暴露断言。

## 验证记录

- `npm run lint`：通过，0 error；257 个 warning（均为 e2e 未使用变量）。
- `npm run type-check`：通过。
- `npm test -- --run`：64 个文件、871 项测试全部通过。
- `npm run build`：生产构建通过，PWA 产物生成成功。
- `npm run lint:design-tokens`：通过。
- `git diff --check`：通过。
- `npm run test:browser:inventory`：73 个 spec、577 个声明、无缺失关键文件、无 `.only`、无 `skip/fixme`；关键覆盖就绪。

按仓库规则，本机没有执行 Playwright、浏览器安装或浏览器服务。`product-ux-trust.spec.ts` 覆盖旧版 925 条收藏数据、Today 诚实空状态、Availability 单页头、独立编辑器壳层；响应式测试覆盖 375、768、1440、2560 四档。已清零的浏览器测试清单仍须通过 GitHub Actions 的 **Hosted browser tests** 工作流完成真机执行确认。

## 兼容性说明

- 未清空、重建或重命名用户 IndexedDB/localStorage 数据；收藏迁移逐项恢复。
- 未移除旧计费项目、发票、绘图、表单等数据模型；它们只从默认入口隐藏。
- 未改变 `.brain` 备份格式和导入/导出路径。
- 未引入账户、服务器、云同步或新的默认联网行为。
