> [!IMPORTANT]
> **历史快照**：本文记录 2026-08-26/27 当时的审计或实施状态，不再作为当前产品说明。2026-08-28 后的信息架构、AI 默认权限、PWA 使用方式等已经继续调整；当前行为以 `main` 代码、根目录 `README.md` 与 `docs/*.md` 为准。

# LifeOS 全项目产品体验、UI 与工作流审计

> 审计日期：2026-08-26<br>
> 审计对象：当前仓库代码、README/产品文档、全部路由与子页面、13 张当前运行截图<br>
> 目标：让 LifeOS 一眼清晰、长期耐看，每个模块都有明确入口、处理过程、完成出口、归档路径，并形成真正促进生活与工作的闭环。

## 0. 先说结论

LifeOS 现在已经具备一套相当完整、也有审美基础的本地优先个人工作台，但它目前更像“许多能力很强的工具被装进了同一个侧栏”，还没有完全成为“一条自然、可信、走得完的个人工作流”。

它的问题不是丑。截图中的浅色主题、留白、圆角、低饱和配色、卡片和图标已经比一般功能型后台更友好。真正拉低体验的是：信息架构没有替用户做足取舍、同一概念出现多套模型、部分指标在无数据时仍给出积极结论、空状态缺少下一步、完成和归档的语义不统一，以及高级/隐藏功能重新渗入主流程。

一句最直接的评价：

> 现在的 LifeOS 是“好看的功能集合”，下一步应把它收敛成“可信的行动闭环”。

基于本次截图和源码审查，我给当前版本的主观产品评分如下。这不是工程质量评分，而是一个普通人持续使用时的体验评分。

| 维度 | 当前判断 | 主要原因 |
| --- | ---: | --- |
| 视觉基础 | 7/10 | 色彩安静、留白充足、组件已有统一感；但层级偏平、卡片过多、低对比、工具栏过密 |
| 第一眼清晰度 | 5/10 | 首页尚可，任务/日程/设置/时间跟踪同时暴露太多能力 |
| 信息架构 | 4.5/10 | 首页/今天/任务/项目/日程/回顾的职责重叠，更多功能形成第二套产品 |
| 工作流闭环 | 4.5/10 | 收集入口不错，但分拣、下一步、完成、复盘、归档之间没有稳定接力 |
| 状态可信度 | 4/10 | 无任务时显示 100% 专注、无样本时显示项目健康/按时率 100% 等，会损害信任 |
| 一致性 | 5/10 | 中英文、Emoji/Lucide、页面名、项目模型、计时模型、删除/归档模型不一致 |
| 可持续使用 | 6/10 | 本地优先、备份、笔记和任务底座很好；但噪声和复杂度会随数据增长放大 |

最值得保留的产品骨架是：**收集 → 安排 → 专注 → 沉淀 → 回顾**。README 已经说对了方向，首次引导也表达得很好；现在需要让每个页面和数据模型都服从这条主线。

---

## 1. 审查范围、证据与限制

### 1.1 本次实际检查了什么

- 逐张查看用户提供的 13 张 2560×1187 当前运行截图。
- 阅读全局布局、侧栏、移动底栏、页头、面包屑、命令入口和首次引导。
- 检查所有正式路由、重定向和异常页：`/`、`/ai`、`/today`、`/tasks`、`/pm`、`/schedule`、`/notes`、`/links`、`/activity`、`/focus`、`/automations`、`/portfolio`、`/energy`、`/retrospective`、`/availability`、`/create`、`/settings`、`/about`、`/privacy` 及绘图/表单/文档实体子路由。
- 检查任务、项目、日历、笔记、收藏、计时、番茄、习惯、自动化、回顾、精力、可用时间、文档、绘图、表单的入口、完成、删除和归档逻辑。
- 阅读 README、功能分层注册表、页面元数据及用户文档，检查产品承诺与实际可见功能是否一致。
- 静态运行仓库允许的 `npm run test:browser:inventory`：现有 72 个浏览器测试文件、572 个源码级测试声明、17 个 skip/fixme，结果为 `Critical coverage ready: no`。

### 1.2 如何区分结论可信度

文中使用三类判断：

- **截图确认**：用户当前运行结果中直接可见。
- **源码确认**：交互、状态或路由可从当前代码明确验证。
- **需要回归**：源码呈现高风险，但仍应在托管浏览器环境用真实交互确认。

根据项目规定，本机禁止运行 Playwright 或浏览器测试，所以本次没有私自启动浏览器自动化。截图之外的页面视觉结论属于“源码级审查”，后续应通过唯一允许的 GitHub Actions `Hosted browser tests` 工作流完成真机回归。

### 1.3 一个重要的测试现实

项目的浏览器测试数量很多，但“数量多”不等于当前体验已经被覆盖：

- 响应式测试多数只验证页面能打开或元素存在，没有验证溢出、遮挡、底部更多菜单可滚动、触控可达性。
- 用户旅程仍包含已经变化的标题和旧导航文案，说明部分断言已落后于当前产品。
- 跨功能集成、备份恢复、自动化、周期任务、Gantt 等关键闭环存在 17 个 skip/fixme。
- 当前截图中的收藏页渲染异常，说明“能访问路由”的测试并没有充分保护真实数据状态下的页面。

因此，后续测试应从“页面存在”升级为“用户能完成一件事并得到正确结果”。

---

## 2. 应该确立的产品定义

建议把 LifeOS 的产品定义固定为：

> LifeOS 是一个本地优先的个人行动与知识闭环：把事情收进来，决定下一步，把时间留给重要事项，完成后沉淀经验，并在回顾中开始下一轮。

这个定义会自然给每个一级页面划清职责：

| 页面 | 唯一主职责 | 不应该承担的职责 |
| --- | --- | --- |
| 首页 | 看全局、发现当前最重要的一步 | 不是所有模块的功能展览馆 |
| 今天 | 执行今天已经决定要做的事 | 不再做复杂项目管理和长期分析 |
| 收件箱 | 接住并分拣尚未决定归属的输入 | 不只是一个“清零”祝贺页 |
| 项目 | 管理有结果、有边界、可完成的目标 | 不应先展示企业级 SPI/资源利用率 |
| 任务 | 管理可执行的下一步和状态 | 不应同时充当习惯、资源、人力平台 |
| 日程 | 管理有具体时间承诺的事件和时间块 | 不应成为发票与计费后台 |
| 笔记 | 沉淀上下文、过程和可复用知识 | 不应在已有笔记时仍显示首次欢迎页 |
| 收藏 | 保存外部参考并转化为可用资料 | 不能是核心导航中唯一直接报错的页 |
| 回顾 | 解释发生了什么、关闭未完成项、决定下一轮 | 不能只是逐条记录自动保存事件 |
| AI 助手 | 在用户明确授权下帮助整理和执行 | 不应抢占日常主线，也不能模糊写入边界 |

### 2.1 六条产品约束

1. **每页只突出一个主动作。** 其他能力降为次级、菜单或渐进展开。
2. **没有数据就显示“暂无数据”，绝不伪造成功。** 0/0 不是 100%，没有基线就不是健康。
3. **每种对象都有生命周期。** 创建、处理中、完成、归档、恢复、永久删除的语义要一致。
4. **同一概念只保留一个名字和一套模型。** 项目、计时、回顾尤其需要统一。
5. **高级功能默认不打扰。** 功能注册表标记为隐藏，就不能从正式页面继续露出。
6. **页面必须把人送到下一步。** 空状态、完成态、错误态都不能成为死胡同。

---

## 3. 13 张截图逐张审查

### 截图 1：首页

**好的部分**

- 品牌、主标题、快速收集和工作流提示构成了比较自然的首页开场。
- “快速记录任务/笔记”是全产品最正确的入口之一，符合先收集、后分类的心智。
- 今日事项、活跃项目、最近笔记把行动与知识放到同一视野，方向正确。

**问题**

- 首屏纵向空间被 Hero、两个输入框和四步说明吃掉，真正要做的事项被推到下方。
- 首页流程卡只有“收集、安排、专注、回顾”，而产品文案是“收集、安排、专注、沉淀、回顾”；“沉淀”消失后，笔记与复盘的关系不完整。
- 活跃项目出现 `unknown`，这是数据语义直接泄漏到用户界面，属于信任问题。
- 今日区和项目区都很稀疏，但卡片边框与大面积留白仍占据注意力。
- 展开的“更多功能”拥有自己的滚动条，和主侧栏形成嵌套滚动。

**建议**

- 把首页首屏收敛为：问候/日期、一个通用快速收集框、一个“现在最值得做”的主卡、收件箱未分拣数量。
- 工作流说明缩成一条可点击的五阶段进度条，首次使用时展开解释，熟悉后不再占首屏。
- 项目卡必须展示真实项目名、目标、下一步和截止状态；无法解析项目时显示“未归属”，不能显示 `unknown`。
- “最近笔记/项目/回顾”用一条横向上下文区承接，不要每块都做成同权重大卡片。

**理由**：首页的价值不是介绍 LifeOS 有什么，而是减少用户决定“下一步去哪”的成本。

### 截图 2：AI 指挥中心

**好的部分**

- 对写操作确认、本地上下文和模型连接状态的解释很克制，安全边界设计是正确的。
- 工具模式与聊天模式分开，历史会话支持归档/恢复/删除，生命周期相对完整。

**问题**

- 空会话时，中间是巨大的无意义空白，能力说明、模式说明、提示词入口彼此分散。
- 左侧能力清单像产品说明书，不像可立即执行的起点。
- “指挥中心”名称比实际需要更重，普通人可能不知道它和首页快速输入、全局命令的区别。

**建议**

- 空状态中央直接给 4–6 个基于当前上下文的可点击起始任务，例如“整理收件箱”“规划今天”“总结本周”“从笔记生成下一步”。
- 把左侧能力说明改为会话上下文/工具状态；产品教育仅在首次进入或帮助抽屉出现。
- 保留显眼的“将读取什么/将修改什么/确认后才写入”，并在每次工具结果后提供“撤销/查看改动对象”。

**理由**：AI 页的核心不是展示能力，而是让用户在安全边界内完成一个具体结果。

### 截图 3：今天

**好的部分**

- 晨间规划、今日焦点、容量、时间线、晚间复盘已经具备一天闭环的正确组件。
- “智能安排”“开始新一天”“结束一天”是很有潜力的仪式化入口。

**问题**

- 无任务时显示“专注 100%”，把无样本包装成满分，截图直接证实了误导。
- 今日任务只统计截止日期恰好为今天的任务；首页却还会考虑逾期、进行中等状态，两页的“今天”定义不一致。
- 当前时间约 14:25，但时间线仍从 01:00 附近显示；源码尝试滚动外层容器，而实际可滚动区域可能在 `DayView` 内部，需要回归确认。
- 顶部同时有 karma、智能安排、开始新一天、完整日程、结束一天，首要动作不明确。
- 空状态“享受你的自由时光”可能掩盖收件箱、逾期或没有日期的开放任务。
- 页面下方同时存在 Daily Review 与四步 Evening Review，职责重复。

**建议**

- 0/0 时显示“暂无计划”或 `—`，并给“从收件箱安排”“添加今日任务”两个入口。
- 建立唯一的“Today Query”：逾期未完成 + 今天到期 + 已明确放入今天 + 正在进行，首页和今天页共用。
- 顶部只保留一个状态驱动的主按钮：早晨显示“规划今天”，白天显示“开始专注”，晚上显示“结束一天”。
- 时间线默认滚到当前时间前 1–2 小时；若没有时间块，先展示紧凑的“未排时任务”，而不是空的 24 小时表。
- 合并 Daily Review 和 Evening Review；晚间复盘必须真的完成改期、移入以后、归档/放弃等操作。
- 移除核心界面的 karma，或把它改成不带奖惩压力的“连续完成天数”，并允许关闭。

**理由**：今天页应像驾驶舱，只回答“现在做什么、做完怎么办”，不能让指标和仪式争抢控制权。

### 截图 4：收件箱

**好的部分**

- “收件箱清零”完成态的情绪是轻松的。

**问题**

- 空状态只有祝贺，没有“快速收集”“查看今天”“返回上次工作”等下一步，是明显死胡同。
- 有内容时，当前分拣只支持优先级、推迟到明天、查看详情和接受；不能就地选择项目、日期、类型或删除/归档。
- “接受”只是把 backlog 变为 todo，不足以完成“安排”。

**建议**

- 每条输入用一个轻量分拣条完成：这是任务还是参考？属于哪个项目？下一步是什么？何时做？不做则归档/删除。
- 提供键盘流：`P` 项目、`D` 日期、`T` 转任务、`N` 转笔记、`A` 归档、`Delete` 删除、`Enter` 接受。
- 清零状态给一个主按钮“回到今天”，一个次按钮“快速记录新想法”。

**理由**：收件箱的完成定义不是“把状态改成 todo”，而是每条输入都被做出可信决定。

### 截图 5：项目管理

**好的部分**

- 项目筛选、截止事项、受阻事项、风险和进度分析具备专业管理能力。

**问题**

- 页面首先呈现 SPI、燃尽、资源利用率、风险矩阵，个人用户却看不到项目列表、项目目标和“创建项目”主入口。
- 截图中只有 3 个任务，仍显示 SPI 1.00、按时完成率 100%、状态稳定；源码确认无计划值/截止样本时默认使用正向值。
- 主页空项目引导到 `/pm`，但 PM 页面本身没有直接创建项目按钮，入口断裂。
- 项目卡、到期任务、受阻任务和最近动态多数不能直接打开精确对象。
- 系统同时存在“工作区项目”和时间跟踪中的“计费项目”两套项目模型，后者带客户与时薪，容易造成重复创建和数据割裂。

**建议**

- 把项目页重做成“项目中心”：进行中、等待中、已完成、已归档四种状态；每张卡只显示目标、下一步、进度、近期里程碑、风险。
- 项目详情页再放任务、笔记、日程、时间记录和可选分析；SPI/燃尽只在用户启用项目管理高级模式且存在基线时展示。
- 无样本显示“数据不足”，绝不显示健康/100%。
- 统一项目模型；若保留计费能力，应明确叫“客户/计费项目”，并作为高级扩展关联到核心项目，而不是并列存在。
- 所有任务和活动行都可打开精确任务；项目空状态直接创建。

**理由**：项目对个人的第一意义是“我要达成什么”，不是“我的指标是否像企业项目办公室”。

### 截图 6：任务

**好的部分**

- 看板、列表、日历、矩阵、分拣、模板、筛选和归档能力很强。
- 任务卡已经包含状态、标签、项目、时间估计等丰富信息。

**问题**

- 顶部工具栏同时出现列设置、归档、看板/列表/日历/矩阵/分拣、模板、视图和过滤，第一眼无法判断最重要操作。
- 五列固定布局在当前高度中只看到卡片开头，浏览和拖拽成本高。
- `PLANNING` 等英文状态混入中文界面；Emoji 与 Lucide 图标混用。
- 任务一级标签还包含“习惯”和“资源”，把不同对象强行塞入任务中心。
- “资源”页仍显示任务导出按钮，语义不匹配。

**建议**

- 默认任务页只保留：视图切换、搜索/筛选、创建任务；模板、列设置、批量、导出、归档收进“更多”。
- 默认视图建议使用“下一步 / 进行中 / 等待 / 完成”四列；Backlog 由收件箱和项目承担。
- 允许用户保存视图，但只提供 2–3 个清晰预设，不把所有视图同屏陈列。
- 把习惯移到“生活”或更多功能，把团队资源从个人默认产品中隐藏。
- 统一中文状态和颜色语义，卡片只常显标题、项目、日期、优先级；其余按需展开。

**理由**：强功能不等于把所有控制同时展示。任务页首先要支持快速扫视和移动下一步。

### 截图 7：日程

**好的部分**

- 月/周/日/议程视图、搜索、日历层、事件分类、导入导出与新建事件功能完整。
- 日历格尺寸和整体留白比较舒适。

**问题**

- 顶部存在两层导航和多排控制，视觉重心在工具栏而不是日期内容。
- `Default / Work / Personal / Health...` 分类使用英文，下方日历可见性又出现 Work/Personal/Birthday，用户难以理解“分类”和“日历层”的区别。
- 迷你日历、搜索、分类、可见性全部常显，空日历也显得复杂。
- “日程”页同时容纳日历、时间跟踪、番茄钟，三种心智差异很大。

**建议**

- 日历默认只显示：今天、前后导航、月/周/日/议程、新建事件；搜索、导入导出、打印放入更多菜单。
- 明确区分“日历”与“颜色分类”：日历是来源/可见层，分类是事件属性；默认只展示一种筛选方式。
- 全部中文化，并允许设置中编辑颜色和名称。
- 时间跟踪保留为高级入口；番茄钟与专注流程合并，不再占日程一级标签。

**理由**：日历的视觉对象应该是时间和承诺，而不是日历工具自身。

### 截图 8：笔记

**好的部分**

- 文件夹、列表、编辑器、每日笔记、图谱、模板、导出、标签、反向链接、版本记录，知识管理底座很强。
- 多种桌面布局和移动抽屉体现了对重度用户的考虑。

**问题**

- 截图左侧已有两篇笔记，右侧仍显示“欢迎使用笔记”，造成“数据在，但系统没有接住我”的感觉。
- 欢迎页只有 `Cmd+N`、`Cmd+K`，没有可点击主按钮；Windows/Linux 用户虽然源码支持 Ctrl，界面只写 Cmd。
- 创建入口主要依赖小图标或列表顶部，空白编辑区没有直接行动。
- 单篇笔记菜单没有归档；归档主要藏在多选批量操作中，单篇删除提示为不可撤销，生命周期不一致。
- 模板变量中的用户名仍硬编码为 `User`，没有使用设置中的显示名称。

**建议**

- 若存在笔记且没有选中项，默认打开最近编辑的非归档笔记；若用户主动关闭选择，再显示“选择一篇笔记”。
- 真正空库时提供“新建空白笔记”和“从模板创建”两个按钮。
- 快捷键显示为 `Ctrl/Cmd` 或根据平台动态显示。
- 单篇菜单补齐归档/取消归档；删除先进入回收站，再允许永久删除。
- 模板使用本地显示名称；每日笔记、会议笔记、项目笔记可以直接关联项目/事件。

**理由**：知识工具最怕“空白却无入口”和“删除不可恢复”，它们会让用户不敢把长期资料交给系统。

### 截图 9：收藏/链接库

**好的部分**

- 错误边界没有擅自清理数据，并提供重试、刷新和备份恢复，这是正确的安全态度。
- 源码中的收藏模块具备文件夹、集合、收藏、归档、最近删除、恢复、永久删除、去重、导入导出，理论生命周期最完整。

**问题**

- 当前核心导航页面直接进入“加载收藏时出现问题”，这是本次最明确的 P0。
- 错误页虽然安全，但用户无法继续添加/查看任何收藏；“技术详情”也不会自动形成可提交的问题报告。
- 正常页面的工具栏同样很密：搜索、视图、排序、方向、导入、导出、AI 分类、查重、批量操作。

**建议**

- 第一优先级复现当前真实数据状态下的渲染异常，特别检查旧日期数据、文件夹迁移和虚拟列表尺寸/行数据。
- 在任何修复或迁移前保留原始数据并增加“导出诊断包”能力，包含错误信息与无敏感内容的 schema 摘要。
- 正常态默认主动作应是“添加链接”；导入、AI 分类、查重放入维护菜单。
- 收藏条目增加“转为任务”“保存到笔记/项目”的出口，让收藏从仓库变为资料流。

**理由**：收藏是一级核心功能，真实数据一打开就报错会伤害用户对所有本地数据的信任。

### 截图 10：回顾

**好的部分**

- 跨模块活动时间线和趋势分析能帮助用户知道发生了什么。
- 日期与模块筛选结构清楚。

**问题**

- 截图中大量重复的“已更新 IPv6知识讲解”把真正有意义的行为淹没，明显是编辑自动保存事件进入了用户回顾流。
- 每条动态只跳到模块首页，而不是对应笔记/任务/事件。
- “回顾”实际是审计日志；没有完成事项、遗留事项、经验、决定和下一步。
- 分析统计模块使用次数，容易鼓励“使用软件”，而不是促进生活与工作结果。

**建议**

- 将此页拆为“回顾”与“活动记录”两个层次：默认回顾展示结果摘要；原始活动日志放到可展开区域。
- 5–10 分钟内同一对象的连续编辑合并为一次“编辑了笔记”，自动保存不记录为用户事件。
- 动态精确跳转到实体；已删除对象显示只读摘要而不是跳到空页面。
- 回顾模板固定回答：完成了什么、没完成什么及处置、学到了什么、精力如何、下一轮三件事。

**理由**：好的回顾应该降低噪声并形成决定，不是复述数据库发生过多少次更新。

### 截图 11：时间跟踪

**好的部分**

- 计时、记录、工时表、汇总和任务关联能力完整。
- 页面已经针对移动端把内部侧栏改为横向标签，说明有响应式意识。

**问题**

- 全局侧栏、日程主标签、时间跟踪内部九分区形成三层导航。
- 发票、报表、计费项目、费率等在功能注册表中标记为隐藏，却在正式页面直接可达。
- 时间跟踪项目是另一套带客户/时薪的数据模型，与核心项目割裂。
- 个人用户看到“发票、工时表、计费率”会误判产品定位。

**建议**

- 默认时间跟踪只保留“计时 / 记录 / 汇总”，项目直接引用核心项目。
- 发票、费率、客户与高级报告真正隐藏到可安装/实验功能，不进入默认构建的可见导航。
- 计时结束必须有闭环：确认描述、关联任务/项目、是否完成任务、补一条结果备注。
- 时间线/汇总更适合作为回顾的证据，不需要在日程页重复占据导航。

**理由**：时间记录应服务于行动与复盘，不能反过来把个人系统变成计费后台。

### 截图 12：设置—通用

**好的部分**

- 备份提醒、本地存储说明、导入导出和主题自定义都符合本地优先定位。
- 设置按基础、工作区、数据与连接、系统分组，方向合理。

**问题**

- 全局侧栏与 14 个设置分类形成双侧栏，页面内仍有大量大卡片和自己的滚动，认知负担很高。
- 标题叫“账户设置”，并要求可选邮箱用于“通知和账户恢复（未来功能）”，与“无账户、不会发送服务器”的当前承诺矛盾。
- 通用页同时放账号、全站偏好、默认视图、仪表盘、布局、组件设置，过长且优先级不清。
- 设置中仍出现团队成员、自定义字段、计费/自动跟踪等企业能力。

**建议**

- “账户设置”改成“个人资料（仅存本机）”；在没有真实账户系统前移除邮箱和账户恢复文案。
- 设置首页改为搜索 + 8 个以内的稳定分类；低频项折叠为高级设置。
- 每个分类顶部显示一句影响范围和“恢复默认”，设置改变后显示明确的保存状态/即时生效说明。
- 团队、计费、复杂资源配置跟随高级功能开关，不默认出现。

**理由**：设置是解释产品边界的地方，任何未来功能文案都不能削弱当前隐私承诺。

### 截图 13：设置—笔记编辑与模板

**好的部分**

- 自定义模板空状态有清晰按钮，是本批截图中做得较好的空状态范例。
- 每日笔记、模板和导出设置聚在同一主题下，有一定逻辑。

**问题**

- 页面内容很长，双侧栏和多层滚动仍然明显。
- 模板设置与笔记页面的模板库之间缺少“去模板库查看效果”的双向入口。
- 预设模板、自定义模板、每日笔记格式的层级容易让新用户误以为必须先配置才能使用。

**建议**

- 默认只展示当前生效的每日笔记格式和自定义模板列表；高级变量、导出细节按需展开。
- 模板行增加“预览、使用、复制、归档”，并能从设置直接创建一篇测试笔记。
- 把真正的模板管理放在笔记工作区，设置只控制默认行为。

**理由**：内容本身属于工作区，设置只应管理默认值；否则用户要在“做事”和“配置做事”之间来回跳。

---

## 4. 全局布局与视觉系统

### 4.1 侧栏：当前最大的信息架构问题

当前侧栏把首页、AI、今天、收件箱、项目、任务、日程、笔记、收藏、回顾全部列为近似同权重，再通过“更多功能”展开 12 个带副标题的高级入口。结果是：

- 一级导航已经很长，高级入口展开后又形成内部滚动。
- 主流程和专业功能没有稳定分组，用户只能记位置，不能理解关系。
- Emoji 导航图标、Lucide 页面图标、彩色状态点风格不统一。
- 页面越多，侧栏越像功能目录，而不是工作流导航。
- 当前移动底栏把“首页、AI、今天、收件箱、更多”作为五项，真正高频的任务/日程/笔记只能进“更多”；这不适合移动场景。

建议的桌面导航：

```text
LifeOS
├─ 首页                         全局概览
├─ 今天                         当前执行
├─ 收件箱  3                    未决输入
├─ 工作
│  ├─ 项目
│  ├─ 任务
│  └─ 日程
├─ 知识
│  ├─ 笔记
│  └─ 收藏
├─ 回顾                         日/周/月闭环
├─ AI 助手                      固定但不抢占主线
└─ 更多
   ├─ 习惯、自动化、精力、可用时间
   ├─ 文档/绘图/表单（若正式启用）
   └─ 实验功能
```

移动底栏建议改为：`今天 / 收件箱 / 任务 / 笔记 / 更多`。首页可由顶部品牌或“更多”进入；AI 用全局助手按钮或“更多”进入。移动端的第一目标是随手记录、看今天、完成任务，而不是浏览完整产品目录。

### 4.2 页头与页面命名

当前页面名称存在多套词汇：

| 路由 | 侧栏名 | 页头/元数据名 | 建议统一名 |
| --- | --- | --- | --- |
| `/` | 首页 | 仪表盘 | 首页 |
| `/pm` | 项目 | 项目管理 | 项目 |
| `/schedule` | 日程 | 时间与规划 | 日程 |
| `/links` | 收藏 | 链接库 | 收藏 |
| `/activity` | 回顾 | 回顾/工作记录 | 回顾（活动记录为子视图） |
| `/create` | 文档中心/创建 | 创建 | 文档（或创作，二选一） |

面包屑也有实现债务：任务页实际使用 `timeline`，面包屑仍判断旧的 `gantt`；实体动态常只链接模块而非对象。建议建立单一的导航注册表，侧栏、页头、面包屑、命令面板和文档都从它读取。

### 4.3 页面骨架

建议只保留三种页面模板：

1. **概览页**：页头 + 一个主动作 + 摘要区 + 异常/下一步。
2. **工作台页**：页头 + 紧凑标签/工具条 + 占满剩余空间的主工作区。
3. **阅读/设置页**：页头 + 适中宽度单栏/双栏内容。

当前需要修复的骨架问题：

- `Availability` 在全局 `Layout` 已有页头的情况下又渲染一次 `PageHeader`，会形成重复页头。
- 绘图编辑器、表单编辑/填写/回复页都在全局布局内使用 `h-screen` 和自己的顶栏，容易产生视口溢出、双顶栏或被全局页头挤压。
- Focus 注释说“无布局包裹”，实际路由仍在 `Layout` 内，只是用 fixed 覆盖；应明确采用真正独立的全屏路由或统一覆盖协议。
- 笔记图谱在笔记页内部再次渲染 `PageContent`，形成不必要的容器嵌套。
- “更多功能”、设置、时间跟踪、笔记三栏都可能引入额外滚动容器。一个页面最多应有一个主要纵向滚动区。

### 4.4 视觉层级

当前视觉不是难看，而是“所有东西都很轻、很圆、很像卡片”，导致主次被抹平。

建议：

- 一级页面背景、内容区和卡片只保留 3 个明度层，不要每个小统计再嵌一层卡片。
- 主动作只使用一种高饱和强调色；次操作用描边/文字，危险操作只在确认处使用红色。
- 正文和次级文字提高对比度，所有常规正文至少满足 WCAG AA；不要依赖极浅灰表示“次要但仍必须读懂”的信息。
- 空状态插图缩小，标题 20–24px 即可；把空间让给直接操作。
- 统一图标：导航与工具优先 Lucide，Emoji 只用于情绪化空状态或祝贺，不作为系统级信息编码。
- 卡片圆角建议减少为两级：普通 12px、重点/模态 16px；避免大量 24–32px 圆角让生产力工具显得松散。
- 宽屏下不要一味拉大留白。任务、日历、笔记应利用宽度；阅读页才限制到 960–1120px。

### 4.5 设计令牌与旧样式

源码中仍存在一批疑似未定义的旧 Tailwind 类，例如 `primary-light`、`primary-dark`、`primary-cyan`、`bg-bg-light-secondary`、`text-text-dark-muted`。它们集中出现在每周回顾、绘图、表单和 Focus 等页面，当前样式表中没有对应定义。

这会造成按钮颜色、选中态、边框或暗色文字在部分页面失效。建议把样式令牌纳入静态检查：不允许页面直接发明新颜色名，所有颜色必须来自当前语义 token 注册表。

### 4.6 响应式与触控

源码中已有不少 `md:` 分支，但当前验证仍偏“能渲染”，没有验证“能完成任务”。重点风险：

- 任务工具栏、日历筛选、表单/绘图顶栏在窄屏可能横向溢出。
- 多个卡片动作只在 `group-hover` 时出现，触屏用户无法发现。
- 移动“更多”菜单包含大量入口，但没有可靠的可视高度/滚动约束。
- Notes 使用运行时 `window.innerWidth` 和 CSS 双重移动分支，实际渲染逻辑重复，窗口变化时也不一定即时同步。
- 三栏笔记、时间跟踪九分区、设置十四分类在平板宽度上会进入尴尬过渡态。

验收不能只看 375/768/1440 页面能打开，还要逐页验证：主动作可见、没有横向滚动、没有遮挡、菜单可触达、软键盘弹起后输入和提交仍可用。

---

## 5. 全页面与子页面审查

下面覆盖截图没有直接展示的页面。表中“当前优点”与“主要问题”均来自源码可验证行为；视觉细节仍需托管浏览器截图回归。

### 5.1 核心页面

| 页面/子视图 | 当前优点 | 主要问题 | 建议与理由 |
| --- | --- | --- | --- |
| 首页高级组件 | 可按需展开天气、组件和统计 | 仍容易变成组件陈列；主页主线会被配置吞没 | 默认只显示行动闭环相关组件，高级小组件由用户主动添加 |
| AI 历史会话 | 支持归档、恢复、删除、工具确认 | 与任务/笔记的产出出口还不够显式 | 每次会话结束提供“保存为笔记/创建任务/仅保留对话”，形成结果 |
| 今天晨间仪式 | 可从 backlog 和逾期任务安排今天 | 与页面主按钮、智能安排并列；定义与首页不完全一致 | 统一 Today Query，并把晨间仪式作为当天首次进入的主流程 |
| 今天焦点 | 能选择重点、时间估计和开始专注 | 完成重点后没有明显“下一项/沉淀结果” | 完成后弹出轻量结果记录，并推荐下一动作 |
| 明日/周计划 | 支持把 backlog 放到未来日期 | 页面过长，执行期仍持续展示规划模块 | 按时间段折叠；白天默认隐藏明日与周计划 |
| 晚间复盘 | 已有完成、未完成处置、反思、关机四步 | “稍后”实际上不改期，“丢弃”只是退回 backlog；与 Daily Review 重复 | 合并复盘并让每个选择真实落地，显示变更预览后提交 |
| 收件箱有数据态 | 支持优先级、稍后、查看、接受 | 不能完成项目/日期/类型决策 | 采用逐条分拣卡和键盘流，一次完成归属和下一步 |
| 任务看板 | 功能完整、可拖拽、详情丰富、支持归档 | 默认密度过高，状态/视图过多 | 用简洁默认视图承接大多数人，高级视图渐进开放 |
| 任务列表/日历/矩阵 | 为不同工作方式提供选择 | 同时暴露导致选择负担；与日程日历重叠 | 首次只推荐看板/列表；任务日历作为视图，不重复事件日历控制 |
| 任务归档 | 归档任务可查看和恢复 | 归档入口被工具栏淹没，完成与归档关系不明确 | 支持完成后 N 天自动归档，Review 中集中确认 |
| 项目上下文下拉 | 全局可切换并管理项目 | 创建入口太间接，`unknown` 可泄漏 | 任何无项目状态都直接创建；所有无法解析值显示“未归属” |
| 日历月视图 | 内容、任务、事件、时间记录能聚合 | 信息层过多，分类与日历层混淆 | 默认只显示事件和时间块；任务只显示明确排程项 |
| 日历周/日视图 | 支持拖拽、时间块和冲突 | 时间线滚动和密度需真机回归 | 自动定位当前时间，全天事件固定顶部，键盘/触控都能编辑 |
| 日历议程 | 更适合扫视近期承诺 | 目前不是默认推荐，也缺少与今天页清晰分工 | 移动端默认议程；今天页只显示今天，日程页管理未来 |
| 笔记每日笔记 | 可按日创建、与日历结合 | 与 Today 的 Daily Note/Review 容易重复 | Today 写入同一篇每日笔记，不再维护独立反思文本孤岛 |
| 笔记图谱 | 支持聚焦、搜索、孤立节点和建链 | 控件很多，空图谱对普通人价值弱；双击才打开笔记不易发现 | 默认展示“相关笔记”和孤立建议，完整图谱作为高级视图 |
| 收藏正常态 | 生命周期和维护能力完整 | 工具栏过密，缺少手动添加主入口和行动转化 | 主入口改为添加链接；维护动作收纳；增加转任务/笔记/项目 |
| 回顾活动分析 | 有热力图、模块使用、趋势 | 衡量操作量而非结果，可能鼓励忙碌 | 统计完成的成果、计划兑现率、开放循环和精力趋势 |

### 5.2 高级页面

| 页面 | 当前优点 | 主要问题 | 建议与理由 |
| --- | --- | --- | --- |
| Focus | 全屏、任务关联、快捷键、减少视觉干扰 | 进入即开始一套 focus session，但计时器又要单独启动；退出可能仍留下时间记录；没有完成任务、干扰记录、结果反思 | 统一为一个计时状态；退出时明确“暂停/结束并记录/放弃”；结束可勾任务、记结果、选下一步 |
| 番茄钟 | 能关联任务、休息、通知并写入时间记录 | 完成专注默认创建 `billable: true`；“番茄钟设置”按钮没有动作；与 Focus 是两套专注模型 | 作为 Focus 的计时模式；默认非计费；设置按钮跳到明确设置页 |
| 时间线/Gantt | 依赖、基线、拖拽和导出能力强 | 对普通项目过重，任务无日期时价值低 | 只在项目详情中按需启用；先引导设置里程碑/日期再打开 |
| 习惯 | 有打卡、连续记录、归档、例程、热力图和分析 | 单页塞入例程、Daily Quests、XP、奖励、徽章、闪卡等隐藏功能；创建表单混入英文；触控依赖 hover | 默认只显示今日习惯、连续记录和备注；例程/分析次级；Quest/XP/闪卡真正隐藏；删除例程需确认 |
| 自动化 | 空状态和规则创建入口清晰，启停和历史具备基础闭环 | 触发器能力和 UI 文案不完全一致；缺少测试运行、复制、编辑和失败恢复 | 增加模板、dry-run、最近一次结果、精确跳转和失败重试；默认只给安全低风险规则 |
| 每周回顾 | 已汇总完成量、时间、习惯、精力，并给出反思框架 | “导出为笔记”实际只复制剪贴板并弹窗；行动项不能创建任务；声称智能洞察但多为模板；旧样式 token 风险 | 真正创建并打开周回顾笔记；行动项一键变任务/安排下周；清楚标明规则生成或 AI 生成 |
| 项目组合 | 卡片/列表/时间线多视图 | 与 PM 项目页重复；空项目可能被判红；卡片和行不可直接进入项目；空状态只提示从别处创建 | 合并为项目页的一种可选视图；无数据为“暂无状态”，所有项目可点击，页面内直接创建 |
| 精力 | 1–10 记录、趋势、热力图、任务能量成本有潜力 | 推荐任务不可直接安排；只看聚合，缺少记录的编辑/删除；“倦怠警报”可能过度医疗化 | 改称“连续低精力提醒”；可查看/编辑记录；推荐项可放入今天或稍后；明确非医疗判断 |
| 可用时间 | 能从日历计算未来 7 天并复制文本 | 重复 PageHeader；设置不持久；开始时间可晚于结束时间；它复制的是文本而非链接 | 修复布局；保存工作时段；校验区间；名称改为“复制空闲时段”，不要称分享链接 |
| 文档中心 | 文档、表格、演示、平台文档能力丰富 | “创建”命名过泛；平台帮助文档与用户内容混在一起；隐藏的表格/演示仍正式暴露；无归档 | 若核心目标是个人工作流，先收敛为“文档”；帮助中心独立；实验编辑器移到 Labs；补归档/回收站 |

### 5.3 文档、绘图、表单及实体子页面

| 页面 | 当前优点 | 主要问题 | 建议与理由 |
| --- | --- | --- | --- |
| 文档编辑器 | 支持文档、表格、演示等复杂创作 | 产品边界过宽，编辑器和全局工作流关联弱；删除多为永久 | 文档必须能关联项目/任务/笔记，结束时有归档；表格/演示保持实验状态 |
| 绘图库 | 模板、搜索、网格/列表、复制和删除完整 | 没有归档；网格操作依赖 hover；隐藏状态与正式入口冲突 | 正式启用前留在 Labs；补归档/回收站和触控菜单 |
| 绘图编辑器 | 工具、形状、图层、自动布局和保存齐全 | 在全局 Layout 内再使用 `h-screen`/顶栏；旧颜色 token；工具栏在小屏风险高 | 使用独立编辑器壳层；统一 token；移动端提供工具抽屉 |
| 表单库 | 创建、复制、填写、编辑、查看回复，基本闭环完整 | 无归档；删除会连回复一起删除；卡片动作依赖 hover；隐藏状态与入口冲突 | 补“关闭收集/归档/回收站”；删除前导出回复；移动端动作常显 |
| 表单构建器 | 字段管理、预览、设置和保存完善 | 双顶栏/h-screen；顶部开关过多；左右 50/50 在窄屏不可用 | 独立编辑器壳层；设置收进侧抽屉；小屏在字段/预览间切换 |
| 表单填写 | 提交完成态明确，可继续填写或返回 | 返回的是内部表单库，不适合作为真正分享表单；旧 token；无隐私/保存提示 | 若仅本机使用应明确“本机表单”；完成后按表单配置决定下一步 |
| 表单回复 | 回复、分析、CSV、单条/全部删除齐全 | `h-screen` 布局；旧 token；“清除全部”风险很高；分析默认可能过度 | 先归档/导出再清空；危险操作二次确认并显示不可恢复范围 |

### 5.4 系统页面与全局能力

| 页面/能力 | 当前优点 | 主要问题 | 建议与理由 |
| --- | --- | --- | --- |
| 首次引导 | 隐私、本地优先、核心循环、主题和备份表达很好 | 最后动作偏“创建内容”，没有让用户完成一次完整小循环 | 引导创建一条输入→分拣到今天→完成→看到回顾，真正教会主线 |
| 全局命令面板 | 搜索和导航效率高 | 与 AI、自然语言输入、快速创建之间边界可能不清 | 命令面板负责“找与做”，AI 负责“理解与协助”，统一入口提示 |
| 设置 14 分类 | 覆盖全面、URL 可直达 | 过多、部分命名不准（内部 id `notes` 实为日历与事件）、高级能力外泄 | 收敛分类、统一命名、设置搜索、按启用功能显示相关设置 |
| 备份/导入导出 | 本地优先最重要的安全网，提示充分 | 数据设置和备份页存在重复导入组件，用户不知该去哪 | 备份负责全量恢复；导入导出负责模块级迁移，避免重复按钮 |
| About | 品牌、原则、版本和构建信息完整 | 与设置中的“关于”重复 | 设置关于只显示摘要并链接独立 About 页 |
| Privacy | 对本地、外部网络、AI 上下文和备份解释清楚 | 与“账户恢复邮箱”文案冲突 | 以 Privacy 为唯一事实来源，设置文案必须与它一致 |
| 404 | 清楚说明数据未受影响，可返回或回首页 | `navigate(-1)` 在直接访问时可能没有有效应用历史 | 无历史时直接回首页；可附常用入口 |
| 错误边界 | 强调不自动删除，提供恢复路径 | 只提供通用重试，缺少对象级诊断和安全模式 | 支持导出诊断、只读/安全模式、明确受影响模块和最近迁移状态 |

### 5.5 设置的 14 个分类

| 设置分类 | 审查结论 | 建议 |
| --- | --- | --- |
| 通用 | 同时包含个人资料、全站偏好、默认视图、仪表盘、布局和组件，首屏过长；“账户/恢复邮箱”与本地优先冲突 | 改成“个人与应用”，移除账户语义；默认视图和仪表盘拆到工作区外观 |
| 外观 | 主题、强调色、自定义 CSS 分层基本合理 | 常用主题与字号在前；自定义 CSS 明确风险、提供安全预览和一键禁用 |
| 通知 | 浏览器权限和日历提醒放在一起符合主题 | 解释通知仅发生在什么条件下；被拒绝时给浏览器级恢复指引；不要暗示服务端推送 |
| 笔记编辑与模板 | 每日笔记、模板、导出相关，但内容属于笔记工作区 | 设置只保留默认值；模板的创建、预览、归档放回笔记模板库 |
| 任务 | 可控制任务管理行为 | 只展示稳定、可解释的默认值；自动归档需说明触发条件、恢复方式和对回顾的影响 |
| 项目 | 有项目相关配置 | 在两套项目模型统一前暂停扩张；项目创建与归档不应主要藏在设置 |
| 日历与事件 | 内部 tab id 仍叫 `notes`，命名债务明显；日历管理和 ICS 导入导出合理 | 改正路由/代码命名；把导入导出入口与日程页“更多”保持一致 |
| 时间跟踪 | 面板、自动追踪等设置完整 | 只有启用时间跟踪后才显示；费率、计费和自动追踪放入高级扩展 |
| AI 提供商 | 本地 key、上下文和连接配置集中，方向正确 | 始终说明请求目的地、发送数据范围、测试连接是否会联网；提供清除密钥 |
| 导入与导出 | 全量、选择性、Markdown、Notion 等能力丰富 | 当前存在多套导入组件，需按“全量迁移/模块导入/格式导入”分组并去重 |
| 备份 | 备份提醒、历史、选项、自动保存和恢复是产品强项 | 明确“自动保存不等于异地备份”；避免与导入导出页重复出现同一 ImportData |
| 键盘快捷键 | 对重度用户有价值 | 支持搜索、冲突检测、按平台显示 Ctrl/Cmd；移动端不显示无法使用的提示 |
| 高级 | 自定义字段、团队成员和存储信息混在一起 | 存储信息归数据；团队/资源能力默认隐藏；高级功能按启用模块动态出现 |
| 关于 | 版本和说明可在设置内找到 | 保留版本摘要即可，产品故事、仓库、License 统一链接到独立 About 页 |

### 5.6 时间跟踪内部九分区

| 分区 | 是否适合默认显示 | 处理建议 |
| --- | --- | --- |
| 计时器 | 是 | 关联核心任务/项目，停止后确认结果 |
| 记录 | 是 | 支持编辑、删除、导出，和回顾精确互链 |
| 工时表 | 可选 | 个人模式改称“周时间表”，不使用团队/雇佣语气 |
| 汇总 | 可选 | 默认只给时间分布；项目计费汇总跟随计费扩展开启 |
| 项目 | 否，当前会制造第二套项目 | 删除独立项目管理，改为核心项目筛选/设置 |
| 发票 | 否 | 按功能注册表真正隐藏 |
| 报表 | 否或高级 | 简单趋势归入回顾，高级报表按需启用 |
| 时间线 | 可选 | 作为记录的一种视图，不必成为同级分区 |
| 设置 | 否，已有全局设置 | 直接链接 `/settings?tab=time`，避免同一设置出现两处 |

### 5.7 文档与测试的一致性

现有用户文档和浏览器测试中仍能看到旧版英文按钮、旧导航名称、旧侧栏结构和旧首次引导文案。例如部分旅程仍寻找 `Tasks`、`Add Task`、`Create Event`、右侧计时器或“仪表盘/创建”等已经变化的入口。

建议：

- 页面名称、路由、侧栏、面包屑和帮助文档共用一个产品词汇表。
- 行为变更在同一改动中更新 README、对应 `docs/` 页面和浏览器旅程。
- 浏览器测试优先使用角色、稳定中文名称和任务结果断言，不依赖脆弱的页面布局细节。
- 对核心主线保留一条不跳过的黄金旅程；高级模块测试可以分层，但不能用大量 skip 掩盖集成断点。

---

## 6. 端到端工作流：哪里断了，怎样接起来

### 6.1 目标主线

```text
外部输入/脑中想法
        ↓
      收件箱 ── 不做 ──→ 归档/删除
        ↓ 分拣
项目结果 ←→ 下一步任务 ←→ 笔记/参考资料
        ↓ 选择今天
      今天计划 ←→ 日程时间块
        ↓
    Focus/番茄执行
        ↓
完成任务 + 记录结果 + 沉淀笔记
        ↓
日回顾 / 周回顾
        ↓
处理遗留项、归档成果、决定下一轮
```

当前各段并不是都缺功能，而是接力关系不够明确。

### 6.2 对象生命周期矩阵

| 对象 | 入口 | 处理中 | 完成定义 | 归档/恢复 | 产出与下一步 | 当前关键缺口 |
| --- | --- | --- | --- | --- | --- | --- |
| 收件箱输入 | 首页/快捷输入 | 分拣 | 已决定类型、归属、日期/下一步 | 应可直接归档 | 去任务、笔记、项目或日程 | 当前“接受”不等于分拣完成 |
| 任务 | 收件箱、项目、今天、AI | todo/in progress/review | 明确 done，必要时记结果 | 已有任务归档与恢复 | 触发下一任务、项目进度、回顾 | 今天定义不一致；完成后缺结果出口 |
| 项目 | 项目中心、任务归属 | active/waiting | 达成结果或明确取消 | 核心项目有归档能力 | 形成成果笔记、回顾和后续项目 | 创建入口隐蔽；两套项目模型；PM 不以结果为中心 |
| 日历事件 | 日程/自然语言/AI | 即将发生/进行中 | 事件结束 | 通常不必归档，保留历史 | 会议笔记、后续任务 | 与任务、时间记录的关系没有统一呈现 |
| 时间记录 | 计时、Focus、番茄 | 运行/暂停 | 停止并确认内容 | 可保留历史/删除 | 任务实际耗时、回顾证据 | 三套计时心智；番茄默认计费 |
| 笔记 | 快速记录、模板、每日笔记 | 编辑/链接/版本 | 没有“完成”，但可形成稳定成果 | 批量归档存在，单篇不直观；删除偏永久 | 关联项目、任务、事件、回顾 | 已有笔记不自动打开；单篇生命周期不完整 |
| 收藏 | 手动/导入 | 分类、收藏、去重 | 已读/已处理应可标记 | 归档、回收站、恢复完整 | 转笔记/任务/项目参考 | 当前页面报错；缺行动转化 |
| 习惯 | 创建/模板 | 每日打卡 | 当日完成；长期可结束 | 习惯可归档 | 周回顾、趋势 | Gamification 噪声过强；例程删除无确认 |
| 自动化 | 模板/创建规则 | 启用、执行历史 | 规则成功产生结果 | 可禁用；缺归档 | 打开结果对象/修复失败 | 缺 dry-run、编辑/复制和恢复路径 |
| 回顾 | 今天结束/每周入口 | 汇总、反思、处置 | 遗留项已决定、下一轮已生成 | 回顾记录应成为笔记并可归档 | 下一周任务、项目决定、知识 | 当前活动日志与真正回顾混为一谈 |
| 文档/绘图/表单 | 文档中心 | 编辑/收集回复 | 文档完成、表单关闭收集 | 多数无归档/回收站 | 关联项目与成果 | 与主工作流弱关联，隐藏状态不一致 |

### 6.3 必须统一的五个关键语义

#### A. “今天”

建立一个共享查询函数，所有页面一致使用：

- 逾期且未完成；
- 截止今天；
- 用户明确安排到今天；
- 当前进行中；
- 排除归档和完成。

首页、Today、日历、智能安排、晨间仪式、晚间复盘全部引用它。这样不会出现首页说有事、Today 却祝你自由的矛盾。

#### B. “完成”和“丢弃”

- 完成：任务结果已经产生，可选写一句结果。
- 稍后：必须选日期或明确进入 Someday，不能什么都不变。
- 移回收件箱：表示还未决定。
- 放弃：应进入已取消/归档，并记录原因，不等于 backlog。
- 删除：进入回收站，永久删除是单独动作。

当前 Evening Review 中“稍后不修改”“丢弃回 backlog”以及 Rollover 中两个不同标签执行同样动作，必须修正。

#### C. “项目”

核心项目只有一套 ID 和状态。时间记录、任务、笔记、日历、回顾都关联这一套。客户、费率、可计费属性若保留，应作为项目的可选财务扩展，而不是第二套项目系统。

#### D. “专注/计时/番茄”

只保留一个活动计时会话：

- Focus 是界面模式；
- 番茄是时长策略；
- 时间记录是结束后产生的数据；
- 三者共享同一个当前任务和同一个开始/暂停/结束状态。

#### E. “回顾/活动记录”

活动记录是证据，回顾是解释与决定。默认回顾只能展示去重后的成果、变化和开放项；原始日志作为审计层按需打开。

---

## 7. 建议的新首页、今天、项目与回顾布局

这些不是最终视觉稿，而是用于约束信息优先级的低保真结构。

### 7.1 首页

```text
早上好，今天是 8 月 26 日                         [快速收集 ⌘/Ctrl+J]

┌──────────────────────── 现在最重要 ────────────────────────┐
│ IPv6 知识讲解 · 项目：网络课程              [开始专注]     │
│ 下一步：完成示例图；预计 35 分钟                              │
└─────────────────────────────────────────────────────────────┘

[收件箱 3 条待分拣]  [今天 4 项 · 1 逾期]  [日程下一场 15:00]

项目进展（最多 3 个）             最近沉淀（笔记/回顾）
```

### 7.2 今天

```text
今天 · 周三             容量 3h/5h               [开始专注]

需要先处理：1 项逾期 / 2 条未分拣                 [处理]

今日三件事
1. [ ] ……  45m
2. [ ] ……  30m
3. [ ] ……  60m

时间安排（默认定位当前时间）
10:00  ……
14:00  ← 现在
15:00  ……

晚上状态切换为：[结束一天]
```

### 7.3 项目

```text
项目                                            [+ 新建项目]
[进行中] [等待] [已完成] [已归档]

网络课程
目标：发布完整 IPv6 教程
下一步：补全示例图                         [继续]
进度：7/12 · 本周里程碑：初稿完成
关联：5 任务 · 3 笔记 · 1 日程

高级分析（有真实基线时才出现）
```

### 7.4 回顾

```text
今日回顾 / 本周回顾

完成的成果（3）              时间与精力（证据，不评分）
开放循环（2）                需要决定：[明天] [稍后] [放弃]
学到的事情                  [追加到每日笔记]
下一轮三件事                [创建并安排任务]

活动记录 61 条（已合并 42 次自动保存）             [展开]
```

---

## 8. 优先级明确的修改清单

### P0：先恢复信任和正确性

#### P0-1 修复收藏页真实数据渲染异常

- 保留数据、备份优先，不做自动清库或粗暴重置。
- 用当前用户数据结构复现，检查迁移、日期反序列化、文件夹树和虚拟列表。
- 加入错误状态回归测试和诊断导出。
- 验收：旧数据、空库、900+ 条数据、归档/回收站四种状态均可进入且无控制台异常。

#### P0-2 清除所有“无数据伪成功”

- Today 0/0 不显示 100%。
- PM 无计划值/截止样本不显示 SPI 1.00、按时率 100%、健康稳定。
- Portfolio 空项目不判红。
- 任何统计使用 `— / 数据不足 / 尚未开始`。
- 验收：所有空状态不会显示成功、失败或健康评价。

#### P0-3 修正直接错误的动作语义

- Evening Review 的“稍后”必须完成改期；“丢弃”进入已取消/归档，不回 backlog。
- Rollover 中“保留 backlog”和“丢弃”不能执行相同代码。
- 番茄记录默认 `billable: false`。
- “番茄钟设置”按钮必须可用或删除。
- 每周回顾“导出为笔记”必须真正创建笔记，不是只复制剪贴板。

#### P0-4 修复页面壳层冲突

- Availability 移除内部重复 PageHeader。
- 绘图、表单编辑/填写/回复改用独立 EditorLayout，不能在全局页头下继续 `h-screen`。
- Focus 明确使用独立全屏壳层，退出时处理活动计时。

#### P0-5 兑现功能分层

- 功能注册表标记 hidden 的发票、报表、计费、Quest、XP、闪卡、表格、演示、绘图、表单不得继续出现在默认正式入口。
- 若产品决定正式启用，先更新注册表、README、导航、帮助文档和完整生命周期，再公开。

#### P0-6 修正本地优先文案冲突

- “账户设置”改为“个人资料（仅存本机）”。
- 没有账户/服务器能力时移除邮箱与“账户恢复”文案。
- 所有可能联网的行为明确说明来源、触发时机和数据范围。

### P1：把主工作流接通

#### P1-1 重做收件箱分拣

让每条输入能在一个界面中转为任务/笔记/收藏，选择项目、日期和下一步，也能归档或删除。清零后回到今天。

#### P1-2 建立共享 Today Query

首页、Today、晨间仪式、智能安排、回顾使用同一组今天/逾期/进行中规则；补单元测试和跨页面浏览器测试。

#### P1-3 合并项目体系

统一核心项目和时间跟踪项目；项目中心负责创建、目标、下一步、完成、归档；PM 分析和 Portfolio 降为项目内部视图。

#### P1-4 合并专注体系

Focus、番茄和时间记录共享活动会话，结束时完成任务、记录结果、保存时长并推荐下一步。

#### P1-5 把 Review 从日志升级为闭环

默认展示结果、遗留决定、沉淀和下一轮；自动保存事件合并；所有对象精确链接；日回顾和周回顾产生真实笔记/任务。

#### P1-6 统一归档和回收站

- 完成不等于归档；完成是业务状态，归档是退出活跃视图。
- 删除默认进入回收站，30 天或手动清空后永久删除。
- 任务、项目、笔记、收藏、习惯、自动化、文档、绘图、表单使用一致的动作词与确认模式。
- 日历事件和时间记录可例外，但要明确“历史保留/删除”。

### P1：同步完成的信息架构与可用性工作

- 按“今天/收件箱/工作/知识/回顾/助手”重组侧栏。
- 移动底栏优先日常行动，而不是 AI 与首页重复占位。
- 统一页面名、面包屑和实体深链。
- 空状态必须有一个主 CTA；错误态有安全恢复；完成态有下一步。
- 所有 hover-only 动作补触控/键盘入口。
- 默认视图最多显示一个主按钮和 3–5 个常用控制。

### P2：视觉收敛与高级能力

- 清理旧颜色 token，建立 design-token lint。
- 减少卡片套卡片、统一圆角/阴影/边框和状态色。
- 提高次级文本对比度并完成 WCAG AA 检查。
- 自动化增加模板、试运行和失败恢复。
- 精力推荐与 Today 可执行联动。
- 图谱/Gantt/项目分析只在有数据和明确需求时出现。
- 文档/绘图/表单在明确产品定位后选择正式启用或彻底进入 Labs。

---

## 9. 推荐实施顺序

### 阶段 A：可靠性与诚实状态

先处理收藏报错、虚假指标、错误动作语义、重复页头/视口冲突、旧 token 和隐私文案。这个阶段不重做视觉，只确保用户看到的每个状态都是真的、每个按钮都兑现承诺。

### 阶段 B：主循环可走通

完成收件箱分拣、统一 Today Query、合并专注计时、重做日/周回顾输出、补齐归档与恢复。阶段结束时，一个新用户应能从输入一条想法开始，走到完成、沉淀和回顾。

### 阶段 C：导航与页面收敛

重组侧栏和移动底栏，统一页面名字，重做项目中心，把 PM/Portfolio/计费项目收敛到一个模型；缩减任务、日程、设置的常显控制。

### 阶段 D：视觉精修和高级模块

在信息架构稳定后再统一视觉 token、密度、响应式和动效，并决定绘图/表单/演示/发票/资源等能力是正式产品、可选扩展还是实验功能。

不建议先做大规模换肤。若工作流和指标语义没有修复，新主题只会让错误变得更漂亮。

---

## 10. 体验验收标准

### 10.1 五秒测试

用户进入任一核心页面 5 秒内，应能回答：

1. 我现在在哪？
2. 这里主要帮我完成什么？
3. 当前最重要的信息是什么？
4. 下一步按钮是哪一个？
5. 做完之后会去哪里？

### 10.2 核心任务测试

以下路径必须在不查帮助的情况下完成：

- 记录想法 → 收件箱分拣 → 放入项目和今天 → 开始专注 → 完成 → 写结果 → 晚间回顾 → 归档。
- 创建项目 → 定义结果 → 添加下一步 → 关联笔记/日程 → 完成项目 → 生成成果笔记 → 归档并可恢复。
- 添加收藏 → 分类 → 转成任务或笔记 → 标记已处理 → 归档 → 从归档恢复。
- 创建每日笔记 → 从事件创建会议记录 → 产生后续任务 → 在回顾中看到结果。
- 创建自动化 → 试运行 → 启用 → 查看精确结果 → 失败后修复/重试 → 禁用/归档。

### 10.3 状态测试

每页至少覆盖：

- 首次使用空状态；
- 有少量真实数据；
- 大量数据；
- 搜索无结果；
- 错误与恢复；
- 完成态；
- 归档/回收站；
- 旧版本迁移数据。

### 10.4 视觉与响应式测试

至少验证 375×812、768×1024、1440×900、2560×1187：

- 无非预期横向滚动；
- 主按钮首屏可见；
- 底部导航和更多菜单不遮内容并可完整滚动；
- 所有 hover 动作可通过触控和键盘完成；
- 弹窗在软键盘出现时仍能看到输入和提交；
- 页面只有一个主要纵向滚动区；
- 深色/浅色、缩放 200%、减少动态效果均可用。

### 10.5 信任与数据安全测试

- 0 样本不显示百分比成功或健康评价。
- 导入、迁移、删除、清空前可备份并明确影响范围。
- 错误边界从不自动删除或覆盖数据。
- 所有 AI 写入操作显示将修改的对象并可取消。
- 本地、网络、账户、通知、API key 文案与实际行为一致。

### 10.6 浏览器测试策略

继续遵守项目规定，只在 GitHub-hosted 的 `Hosted browser tests` 工作流执行浏览器测试。建议新增/升级这些验收：

- 用旧版本收藏数据打开 `/links`，验证不崩溃。
- 验证 Today 0/0 显示 `—` 而不是 100%。
- 验证首页与 Today 的任务集合一致。
- 验证晚间“稍后/放弃”真正产生不同状态。
- 验证 Focus/番茄/时间记录不会重复计时，且默认非计费。
- 验证 Availability 只有一个页头。
- 验证表单/绘图编辑器不产生双顶栏和页面溢出。
- 验证移动更多菜单所有入口可触达。
- 验证活动日志合并自动保存并能精确打开对象。
- 清理现有 17 个 skip/fixme，优先跨功能、备份恢复和周期任务。

---

## 11. 建议暂时不要做的事

- 不要继续增加一级导航和首页组件。
- 不要把更多企业指标默认推给个人用户。
- 不要在项目模型未统一前继续扩展客户、费率、发票或资源分配。
- 不要在归档/回收站规则未统一前增加更多内容类型。
- 不要用动画、渐变和更大插图掩盖空状态缺少动作的问题。
- 不要为了重构体验破坏 IndexedDB、本地存储或 `.brain` 备份兼容性。
- 不要把本地优先变成账号优先或默认云同步；这正是 LifeOS 最有价值的差异。

---

## 12. 最终建议

LifeOS 最强的不是某一个炫目的模块，而是它已经拥有把任务、时间、项目、笔记、收藏、习惯和回顾连接起来的技术基础。下一步产品工作应做减法和接线：

1. 先让所有状态诚实、所有核心页可靠。
2. 再让收件箱、今天、项目、专注、笔记和回顾真正互相交接。
3. 然后统一项目、计时、完成、归档和删除的语言与数据模型。
4. 最后再精修视觉，并决定高级功能哪些值得正式存在。

做到这四步后，用户看到的将不再是“我可以去 20 个页面做很多事”，而是：

> 我知道现在要做什么；做完后系统会接住结果；以后需要时我能找回来；每一轮都比上一轮更清楚。

这才会让 LifeOS 从一个功能丰富的本地工作台，变成真正好看、好用、可信、能长期陪伴人的个人助手。

---

## 附录 A：源码证据索引

本次结论主要对应以下实现区域，便于后续拆任务：

- 路由与全局壳层：`src/App.tsx`、`src/components/Layout.tsx`
- 导航与页面命名：`src/components/Sidebar.tsx`、`src/components/BottomNav.tsx`、`src/components/PageHeader.tsx`、`src/components/Breadcrumbs.tsx`、`src/config/pageMetadata.ts`
- 功能分层：`src/config/features.ts`
- 首页与 AI：`src/pages/Dashboard.tsx`、`src/components/dashboard/HomeOverview.tsx`、`src/pages/AICommandCenterPage.tsx`
- 今天与回顾：`src/pages/Today.tsx`、`src/components/today/*`、`src/pages/ActivityFeed.tsx`、`src/pages/WeeklyRetrospective.tsx`
- 任务与项目：`src/pages/Tasks.tsx`、`src/widgets/Kanban/*`、`src/pages/PMDashboard.tsx`、`src/pages/Portfolio.tsx`、`src/components/pm/*`
- 日历、计时与专注：`src/pages/TimeTracking.tsx`、`src/components/TimeEntryCalendar.tsx`、`src/components/PomodoroTimer.tsx`、`src/pages/Focus.tsx`
- 笔记与图谱：`src/pages/Notes.tsx`、`src/components/notes/*`、`src/widgets/NotesEditor.tsx`、`src/pages/GraphView.tsx`
- 收藏：`src/pages/LinkLibrary.tsx`、`src/components/CollectionSidebar.tsx`、`src/stores/useLinkLibraryStore.ts`
- 习惯、自动化、精力和可用时间：`src/pages/Habits.tsx`、`src/pages/Automations.tsx`、`src/pages/Energy.tsx`、`src/pages/Availability.tsx`
- 文档/绘图/表单：`src/pages/Docs.tsx`、`src/pages/Diagrams.tsx`、`src/pages/DiagramEditor.tsx`、`src/pages/Forms.tsx`、`src/pages/FormBuilder.tsx`、`src/pages/FormFiller.tsx`、`src/pages/FormResponses.tsx`
- 设置与隐私：`src/pages/Settings.tsx`、`src/widgets/Settings/*`、`src/pages/About.tsx`、`src/pages/Privacy.tsx`
- 测试清单：`scripts/check-browser-test-inventory.mjs`、`tests/e2e/*`

## 附录 B：整改完成的判断句

当下面这些话都能成立时，这一轮产品整改才算完成：

- 首页没有向我介绍所有功能，而是告诉我下一步。
- 今天没有用虚假分数评价我，而是帮助我完成现实中的一天。
- 收件箱中的每条输入都能被决定，而不是只被换一个状态。
- 项目首先告诉我想达成什么，然后才谈指标。
- 任务页强大，但第一次打开并不吓人。
- 日程让我看到时间，而不是看到一排日历工具。
- 笔记在我有内容时自动接住我，删除也不会让我害怕。
- 收藏不会崩溃，而且能把资料送回行动与知识。
- 专注、番茄和计时说的是同一件事。
- 回顾会帮我关闭开放循环，而不是让我阅读自动保存日志。
- 高级功能只有在我需要时才出现。
- 每一类内容都知道如何开始、怎样结束、去哪里归档、如何恢复。

## 附录 C：路由与可见子页面覆盖矩阵

| 路由/入口 | 页面与子视图 | 本次审查方式 | 结论位置 |
| --- | --- | --- | --- |
| 全局 | Layout、桌面侧栏、移动底栏、页头、面包屑、命令入口、Toast、错误边界 | 截图 + 源码 | 第 4、5.4 节 |
| 首次启动 | 四步 Onboarding、备份引导、跳过/提醒 | 源码 | 第 5.4 节 |
| `/` | 首页、快速任务/笔记、今日、项目、最近笔记、高级组件 | 截图 1 + 源码 | 截图 1、第 5.1 节 |
| `/ai` | 聊天、工具模式、上下文、历史会话、归档/恢复 | 截图 2 + 源码 | 截图 2、第 5.1 节 |
| `/today` | 晨间、焦点、容量、指标、时间线、明日/周计划、日/晚回顾、Rollover | 截图 3 + 源码 | 截图 3、第 5.1、6 节 |
| `/tasks?tab=inbox` | 收件箱空状态与逐条分拣 | 截图 4 + 源码 | 截图 4、第 5.1、6 节 |
| `/tasks?tab=tasks` | 看板、列表、日历、矩阵、分拣、模板、筛选、归档 | 截图 6 + 源码 | 截图 6、第 5.1 节 |
| `/tasks?tab=timeline` | Gantt、依赖、基线、拖拽、导出 | 源码 | 第 5.2 节 |
| `/tasks?tab=habits` | 习惯、例程、热力图、分析、Quest/XP/奖励/徽章/闪卡 | 源码 | 第 5.2 节 |
| `/tasks?tab=resources` | 资源利用率与任务资源提示 | 源码 | 截图 6、第 5.1 节 |
| `/pm` | 项目筛选、健康度、燃尽、资源、截止、动态、受阻、风险 | 截图 5 + 源码 | 截图 5、第 5.1、6 节 |
| `/portfolio` | 项目卡片、列表、时间线 | 源码 | 第 5.2 节 |
| `/schedule?tab=calendar` | 月/周/日/议程、事件、搜索、日历层、分类、导入导出 | 截图 7 + 源码 | 截图 7、第 5.1 节 |
| `/schedule?tab=timer` | 计时、记录、工时表、汇总、项目、发票、报表、时间线、设置 | 截图 11 + 源码 | 截图 11、第 5.6 节 |
| `/schedule?tab=pomodoro` | 任务关联、专注/休息、记录、设置入口 | 源码 | 第 5.2、6 节 |
| `/focus` | 全屏专注、会话、任务、计时与退出 | 源码 | 第 5.2、6 节 |
| `/notes?tab=notes` | 三种布局、文件夹、列表、编辑器、标签、模板、附件、反链、事件、版本 | 截图 8 + 源码 | 截图 8、第 5.1、6 节 |
| `/notes?tab=daily` | 每日笔记日历与创建 | 源码 | 第 5.1 节 |
| `/notes?tab=graph` | 搜索、聚焦、着色、孤立节点、建链 | 源码 | 第 5.1 节 |
| `/links` | 集合/文件夹、收藏、归档、回收站、导入导出、AI 分类、查重 | 截图 9 + 源码 | 截图 9、第 5.1、6 节 |
| `/activity` | 动态、日期/模块筛选、分析 | 截图 10 + 源码 | 截图 10、第 5.1、6 节 |
| `/retrospective` | 每周汇总、洞察、反思、行动项、导出 | 源码 | 第 5.2、6 节 |
| `/automations` | 空状态、规则列表、构建器、历史、启停/删除 | 源码 | 第 5.2、6 节 |
| `/energy` | 精力记录、趋势、热力图、提醒、任务建议 | 源码 | 第 5.2、6 节 |
| `/availability` | 工作时段、未来七天空闲、复制文本 | 源码 | 第 5.2 节 |
| `/create` | 文档、电子表格、演示文稿、平台文档 | 源码 | 第 5.2、5.3 节 |
| `/create?tab=diagrams` | 绘图库、模板、搜索、网格/列表、复制/删除 | 源码 | 第 5.3 节 |
| `/diagrams/:id` | 绘图编辑器、工具、画布、形状、图层、布局 | 源码 | 第 5.3 节 |
| `/create?tab=forms` | 表单库、复制、编辑、填写、回复、删除 | 源码 | 第 5.3 节 |
| `/forms/:id/edit` | 表单字段、设置、预览、保存 | 源码 | 第 5.3 节 |
| `/forms/:id/fill` | 填写、提交完成、继续填写/返回 | 源码 | 第 5.3 节 |
| `/forms/:id/responses` | 回复、分析、CSV、单删/清空 | 源码 | 第 5.3 节 |
| `/settings` | 通用、外观、通知、编辑、任务、项目、日历、时间、AI、数据、备份、快捷键、高级、关于 | 截图 12–13 + 源码 | 截图 12–13、第 5.5 节 |
| `/about` | 品牌、原则、版本、构建、仓库/License | 源码 | 第 5.4 节 |
| `/privacy` | 本地存储、联网边界、AI、数据控制 | 源码 | 第 5.4 节 |
| `*` | 404、返回、回首页 | 源码 | 第 5.4 节 |
| 兼容重定向 | `/graph`、`/diagrams`、`/forms`、`/habits`、`/calendar`、`/docs` | 源码 | 第 1、4.2 节 |
